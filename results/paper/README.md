# Tier C: Paper Reported

Paper values are an evidence catalog, not the primary leaderboard.
Each record requires a citation and exact table, figure, or page locator.
Only values jointly measured inside one compatible paper cohort may share a table.
