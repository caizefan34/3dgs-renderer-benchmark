# Tier A: Measured

Only the local benchmark CLI writes here.
A rankable `metrics.json` requires a complete suite identity, pinned renderer commit, hashed raw samples, full environment, performance metrics, and GT quality metrics from the same case.
