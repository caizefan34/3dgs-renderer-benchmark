# Warp-cooperative emit: 13-scene status

Status: **NOT RUN — required artifacts unavailable in the frozen audited environment.**

The Reference V1 result store contains mature checkpoints only for
`room`, `bicycle`, and `garden`; the official data root likewise contains only
those three `cameras.json` datasets. Searches found no mature checkpoints or
official camera data for flowers, stump, treehill, counter, kitchen, bonsai,
train, truck, drjohnson, or playroom. Therefore a 13-scene value, geomean, and
faster/neutral/slower count cannot be reported honestly.

No substitute checkpoint, synthetic camera, or mixed candidate configuration
was used. This is an availability limitation, not a negative performance
result.
