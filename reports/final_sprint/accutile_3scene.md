# AccuTile three-scene benchmark

Status: `NOT RUN — A0 correctness failure`.

The workload opportunity is large (44.49%–51.58% fewer proposed pairs), but the
upstream branch removed tiles that contain above-threshold Reference V1 pixels.
Benchmarking emit, sort, raster, memory, or forward time would therefore measure
a lossy renderer and is intentionally prohibited by the protocol.
