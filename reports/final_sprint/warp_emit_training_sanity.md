# Warp-cooperative emit training sanity

Status: **NOT RUN**.

The requested training sanity is conditional on a positive 13-scene mature
checkpoint validation. That prerequisite cannot be satisfied because only the
three audited checkpoint/data pairs are present in the frozen environment (see
`warp_emit_13scene.md`). No training trajectory claim is made from inference
measurements alone.

The structure, render, and single-loss backward parity gates passed on the
available three scenes and two real cameras per scene.
