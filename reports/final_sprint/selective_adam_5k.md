# SelectiveAdam paired 5K training

Status: **NOT RUN**.

The prerequisite three-scene Reference V1 microbenchmark did not pass the
implementation/semantic gate. Launching paired 5K training would require a
new masked-optimizer integration or the forbidden HiGS path, so it was not
authorized by this ablation's scope.

No wall time, iterations/s, phase timing, Gaussian-count trajectory, PSNR,
SSIM, LPIPS, loss trajectory, or stability result is claimed.
