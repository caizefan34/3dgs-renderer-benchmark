# Generated Reports

Run `benchmark report` to generate `ranking.json`, `ranking.csv`, `ranking.md`, and tier-specific FPS–PSNR/FPS–LPIPS SVG charts.
Tier A is selected as preferred evidence when available, followed by Tier B and then Tier C; the underlying ranking tables remain separate.
