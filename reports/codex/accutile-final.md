# AccuTile final

`CODEX-A = DROP`

The frozen active gsplat baseline lacks an equivalent AccuTile mechanism.  A minimal
isolated conservative backport compiles and preserves the small parity test, and a
Room probe reduced intersections by 59.75%.  It is nevertheless not promotable:
the required 3-scene real-workload protocol, 100-sample timing decomposition, and
full backward parity suite cannot be completed because this host has no bicycle or
garden frozen checkpoints.  No performance or training-semantics conclusion is made.

No commit was created: the implementation is deliberately left uncommitted until the
required validation gate is complete.
