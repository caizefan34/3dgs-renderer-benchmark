# 13 · CANDIDATE C 证书（R3 / R3.1 / R4）

## 标准叙事（R3 → R3.1 → R4）

1. **R3 审计**：对候选 C 的 6/8 梯度族逐项做证书检查，得到 **7,494 违规**。
   （`reports/r3/candidate-c-r3-audit.md` -> 摘要 `reports/r3/candidate-c-r3-final.md`）
2. **R3.1 修复**：定位到根本原因后修正公式（用 RGB 差代替 conic-as-color 代理），
   修复后重跑 90 次测量，**违规数为 0**；最坏比率 **0.9998**（余量 0.0002），
   `VEC` 门限（<= 1e-5 的相对误差）**通过**。
3. **R4 状态**：CUDA 内核（`r4-*.cu` / `ext_skip.cpp` / 构建脚本）已写但**未编译、未在
   GPU 上验证**；已完成的单元测试覆盖 Python 侧。因此 R4 结论为
   **"实现完成，运行时验证未完成"**。

## 关键数字

| 指标 | 值 |
|---|---|
| R3 违规总数 | 7,494（tiling 2,373 + matrix 636 + attr 3,900 + helper 585，见族表） |
| R3.1 修复后违规 | 0 / 90 测量（全部 8 族） |
| 修复后最坏｜g｜/B | 0.9998（余量 0.0002） |
| VEC gate | PASS |
| 62.5% weighted-work | 在 final budget 5% 时的预期加权工作量节省 |

## 重要警示（写报告时不要犯）

1. 不要把 62.5% 写成"推理加速 62.5%"——它是 **weighted-work（计算工作量）节省份额**，
   且只在 5% budget 下成立。
2. 不要把 R3 的 7,494 违规当作"候选 C 失败"——它**只证明 naive 计算器不完整**；
   R3.1 修复后的 0 违规才是决策依据。
3. R4 未验证 = 不能声称"实现了端到端加速"。
4. 文档日期显示 2025-09-18 是有意保留的 **系统性笔误**（正确年份 2026），
   请在最终报告中写 2026。
